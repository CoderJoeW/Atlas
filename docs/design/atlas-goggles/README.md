# Atlas Goggles

Headgear that floats a readout beside the Atlas block the wearer looks at. It replaced the Atlas
Wrench and its inspector dialog.

## Look

Two blocky square lens housings on a bridge, in the shared Atlas language: charcoal armoured metal,
brushed steel rims with a bolt in each corner, and amber glass lenses (`#FFB030`) glowing from within.
A dark rubberised strap with steel rivets goes around the head. Amber matches the item's gold name and
the power system's glow.

## Art pipeline

Images are generated by hand in Nano Banana Pro (1:1, 2K). The style reference is
`docs/design/small-solar-panel/references/power-system-reactive-states.jpg`. Sources are kept in
`raw/`, downscaled to 1024 px.

The icon is rendered on flat `#FF00FF` and chroma-keyed to a 512×512 RGBA PNG. The keyer takes alpha
from each pixel's distance to the sampled backdrop, un-mixes the backdrop from soft edges, despills
again after a premultiplied resize, and keeps only the largest blob. The last despill matters: without
it, the resize leaves a purple fringe on the outline.

| Asset | Source | Pack path |
|---|---|---|
| Inventory icon | `raw/icon.jpeg` | `textures/item/custom/atlas_goggles.png` |

The item is a plain Paper item whose `item_model` is `atlas:atlas_goggles`. The item definition
(`assets/atlas/items/atlas_goggles.json`) and model are hand-placed in the pack, not generated by
CraftEngine, and `GogglesAssetsTest` walks that chain.

## Still to come

- **A 3D worn model.** A head-slot item with no equipment asset draws its own item model on the head,
  so one blocky model can serve the head and the hand, while the gui keeps the flat icon via a
  `display_context` select. It needs three textures: housing, lens and strap.
- **A camera overlay.** Optional thin amber corner brackets, set as the `Equippable` `camera_overlay`.

## Prompts

### Inventory icon

```
Minecraft-style inventory icon of a pair of industrial tech goggles, three-quarter front view from
slightly above. Two chunky SQUARE lens housings side by side, joined by a short bridge, built from
dark charcoal armoured metal with brushed steel rims and a small steel bolt at each lens corner. The
lenses are square panes of amber glass (#FFB030) glowing softly from within. A thick dark charcoal
rubberised strap with small steel rivets loops back behind the lenses. Blocky, chunky proportions
that belong beside Minecraft blocks: hard edges, no round lenses, no organic curves. Match the style
of the attached reference: charcoal housing, steel rails and bolts, amber energy glow.

The goggles are centred and fill about 80% of the frame. Background: perfectly flat, uniform pure
magenta #FF00FF from edge to edge. No shadow, no drop shadow, no ground plane, no reflection, no
vignette, no gradient, no text, no border. No magenta anywhere on the goggles.
```
